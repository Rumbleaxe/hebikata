# Core dependencies for HebiKata
# Streamlit Cloud compatible (no Windows-only packages)

# Core app
streamlit>=1.45.1
pyyaml>=6.0

# Testing (for exercise validation)
pytest>=8.3.5

# Code editor component
streamlit-code-editor>=0.1.22

# Session persistence (localStorage)
streamlit-js-eval>=0.2.70
